cmake_minimum_required(VERSION 3.10)

# Can be built/tested on its own, without OpenToonz, Qt, or a graphics context.
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
    project(OpenToonzGlb LANGUAGES CXX)
    set(GLB_STANDALONE ON)
else()
    set(GLB_STANDALONE OFF)
endif()

add_library(otglb STATIC glbloader.cpp glbloader.h glbpose.cpp glbpose.h glbrenderer.cpp glbrenderer.h)
target_compile_features(otglb PUBLIC cxx_std_17)
target_include_directories(otglb PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
set_target_properties(otglb PROPERTIES POSITION_INDEPENDENT_CODE ON)

option(BUILD_GLB_LOADER_TOOLS "Build standalone GLB inspection and tests" ${GLB_STANDALONE})
if(BUILD_GLB_LOADER_TOOLS)
    enable_testing()
    add_executable(glb_inspect glb_inspect.cpp)
    target_link_libraries(glb_inspect PRIVATE otglb)
    add_executable(glb_render glb_render.cpp)
    target_link_libraries(glb_render PRIVATE otglb)
    add_executable(glbloader_test tests/glbloader_test.cpp)
    target_link_libraries(glbloader_test PRIVATE otglb)
    add_test(NAME glbloader COMMAND glbloader_test)
    add_executable(glbanimationdata_test tests/glbanimationdata_test.cpp)
    target_link_libraries(glbanimationdata_test PRIVATE otglb)
    add_test(NAME glbanimationdata COMMAND glbanimationdata_test)
    add_executable(glbpose_test tests/glbpose_test.cpp)
    target_link_libraries(glbpose_test PRIVATE otglb)
    add_test(NAME glbpose COMMAND glbpose_test)
    add_executable(glblighting_test tests/glblighting_test.cpp)
    target_link_libraries(glblighting_test PRIVATE otglb)
    add_test(NAME glblighting COMMAND glblighting_test)
    find_package(Threads REQUIRED)
    add_executable(glbrenderer_test tests/glbrenderer_test.cpp)
    target_link_libraries(glbrenderer_test PRIVATE otglb Threads::Threads)
    add_test(NAME glbrenderer COMMAND glbrenderer_test)
endif()
